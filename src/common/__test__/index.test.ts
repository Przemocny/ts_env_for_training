import Base from '../'
describe('some tests',()=>{
    test('some test case', ()=>{
        const a = new Base()
        expect(a.name).toBe('Base')
    })

    test('some test case', ()=>{
        const a = new Base()
        expect(a.name).toBe('Base')
    })
})